import { describe, expect, it } from 'vitest';

// Tests for the napi-rs bindings.
// Run `vp test` in this package after building the native module.

describe('@vue-oxlint/plugin', () => {
  it.todo('parse a basic Vue SFC');
});
